import React, { useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  ScrollView,
  TouchableOpacity,
} from 'react-native';

const timeTableTest = () => {
  return (
    <ScrollView>
      <Text>ㅇㅇ</Text>
    </ScrollView>
  );
};
